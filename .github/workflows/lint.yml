name: lint

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # ruff ÉPINGLÉ : installé sans version, une nouvelle publication de
      # l'outil ajoute des règles et fait rougir `main` sans qu'une ligne de
      # code ait bougé — constaté le 2026-09-14 (18 défauts apparus seuls).
      - run: pip install ruff==0.16.5 pyyaml
      - name: ruff
        run: ruff check provisioning
      - name: compile
        run: python -m compileall -q provisioning
      - name: validate YAML
        run: |
          python - <<'PY'
          import pathlib, yaml
          for p in [pathlib.Path("compose.tuning.yml"), pathlib.Path("opencti/docker-compose.yml")] + list(pathlib.Path("provisioning").rglob("*.yaml")):
              yaml.safe_load(p.read_text()); print("ok", p)
          PY
